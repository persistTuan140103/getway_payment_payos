version: "1.0"
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command:
    - cd display_payment_app
    - flask run --host=0.0.0.0 --port=5000 -p app_flashsale
  network:
    port: 5000

    