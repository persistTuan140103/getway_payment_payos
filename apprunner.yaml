version: "1.0"
runtime: python311
build:
  commands:
    build:
      - "pip install -r requirements.txt"
run:
  pre-run:
    - "cd display_payment_app"
  command: "py app_flashsale.py"
  network:
    port: 5000

    