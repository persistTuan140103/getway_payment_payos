version: "1.0"
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: gunicorn display_payment_app.app_flashsale:app
  network:
    port: 8000
port: 8000
