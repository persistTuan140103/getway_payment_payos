version: "1.0"
runtime: python311
build:
  commands:
    build:
      - echo "Installing dependencies..."
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: "py display_payment_app/app_flashsale.py"
  network:
    port: 5000

    