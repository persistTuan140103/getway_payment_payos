version: "1.0"
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: gunicorn -w 4 display_payment_app.app_flashsale:app --bind=0.0.0.0:8080
  network: 
    port: 8080
    env: PORT
