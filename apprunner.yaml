version: "1.0"
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: "python app_flashsale.py"
  network:
    port: 5000
