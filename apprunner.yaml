version: "1.0"
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: gunicorn -w 4 -b 0.0.0.0:5000 display_payment_app.app_flashsale:app
  network:
    port: 5000
