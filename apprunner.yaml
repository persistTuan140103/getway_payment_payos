version: "1.0"
runtime: python311
build:
  commands:
    build:
      - "pip install -r requirements.txt"
run:
  pre-run:
    - "cd display_payment_app"
  command: "flask run --host=0.0.0.0 --port=5000 -p app_flashsale"
  network:
    port: 5000

    